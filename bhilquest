#!/bin/bash
# BhilQuest - Raccourci de Lancement (Linux)
# Démarre le jeu BhilQuest

# Récupère le répertoire où se trouve ce script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Exécute le lanceur Python dans le bon répertoire
cd "$DIR"
python3 launcher.py
